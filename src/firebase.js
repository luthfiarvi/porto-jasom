// Firebase disabled (template mode)

export const auth = {};
export const db = {};

export const loginWithGoogle = () => {
  console.log("Firebase disabled");
};

export const logout = () => {
  console.log("Firebase disabled");
};
